export type EmployeeRow = { id: string; employeeCode: string; firstName: string; lastName: string; name: string; photoUrl: string | null; email: string | null; mobile: string | null; status: string; employmentType: string; joiningDate: string; branchId: string; branchName: string | null; departmentId: string | null; departmentName: string | null; designationId: string | null; designationName: string | null; reportingManagerId: string | null; managerName: string | null; userId: string | null; deviceUserId: string | null };
export type Doc = { id: string; type: string; title: string; fileName: string; mimeType: string | null; sizeBytes: number | null; expiryDate: string | null; verification: string; createdAt: string };
export type EmployeeDetail = EmployeeRow & Record<string, any> & { bank: Record<string, string | null> | null; documents: Doc[]; transfers: { id: string; fromBranchId: string | null; toBranchId: string; transferDate: string; reason: string | null }[]; history: { id: string; fromStatus: string | null; toStatus: string; effectiveDate: string; note: string | null }[]; reports: { id: string; name: string; designationName: string | null }[] };
export type Dept = { id: string; name: string; code: string | null; branchId: string | null; isActive: boolean; employeeCount: number };
export type Desig = { id: string; name: string; level: number; isActive: boolean };
export type Branch = { id: string; name: string; status: string };
export const STATUS_LABEL: Record<string, string> = { active: "Active", inactive: "Inactive", probation: "Probation", notice_period: "Notice period", resigned: "Resigned", terminated: "Terminated" };
export const EMP_TYPE: Record<string, string> = { full_time: "Full time", part_time: "Part time", intern: "Intern", contract: "Contract", freelancer: "Freelancer", consultant: "Consultant" };
export const DOC_TYPES: Record<string, string> = { aadhaar: "Aadhaar", pan: "PAN", passport: "Passport", driving_license: "Driving license", resume: "Resume", offer_letter: "Offer letter", joining_letter: "Joining letter", experience_letter: "Experience letter", certificate: "Certificate", other: "Other" };
