export type LeaveType = { id: string; name: string; code: string; isPaid: boolean; kind: string; annualQuota: string; accrual: string; carryForwardMax: string; encashable: boolean; allowHalfDay: boolean; allowNegative: boolean; approvalLevels: number; minNoticeDays: number; maxConsecutiveDays: number | null; applicableGenders: string[] | null; probationAllowed: boolean; isActive: boolean; sortOrder: number };
export type Balance = { id: string; allocated: string; carriedForward: string; adjusted: string; used: string; encashed: string; available: number; type: LeaveType };
export type LeaveReq = { id: string; employeeId: string; employeeName: string; employeeCode: string; departmentName: string | null; branchName: string | null; typeName: string; typeCode: string; typeKind: string; isPaid: boolean; approvalLevels: number; fromDate: string; toDate: string; halfDay: string | null; days: string; reason: string | null; status: string; approvals: { level: number; byName: string; action: string; note?: string; at: string }[]; rejectionReason: string | null; createdAt: string; reportingManagerId: string | null };
export const LSTATUS: Record<string, string> = { pending: "Pending", manager_approved: "Manager approved", approved: "Approved", rejected: "Rejected", cancelled: "Cancelled" };
export const ltone: Record<string, string> = { pending: "pending", manager_approved: "under_review", approved: "approved", rejected: "rejected", cancelled: "cancelled" };
